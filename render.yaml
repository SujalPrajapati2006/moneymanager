services:
  - type: web
    name: money-manager
    runtime: java
    rootDir: money-manager

    buildCommand: ./mvnw clean package -DskipTests

    startCommand: java -jar target/money-manager-0.0.1-SNAPSHOT.jar

    envVars:
      - key: SPRING_PROFILES_ACTIVE
        value: prod